STATUS: HISTORISCH / ARCHIVIERT — keine Quelle der Wahrheit mehr

Die Dateien in diesem Ordner (planner-architecture-v1.md,
planner-module-registry.json) sind der Stand VOR der Umstellung auf die
datei-basierte Spezifikationsstruktur. Sie werden nicht mehr gepflegt
und können veraltete Regelzahlen (z. B. "R1-R15" statt R1-R19) oder
veraltete compatibleWith-Angaben enthalten.

Aktuelle, verbindliche Quelle ist ausschließlich:

  planner-specs/architecture/planner-architecture.md
  planner-specs/architecture/planner-registry.json
  planner-specs/architecture/module-lifecycle.md
  planner-specs/architecture/decisions.md

Dieser Ordner bleibt nur aus historischen Gründen erhalten (Nachvoll-
ziehbarkeit, was vor der Umstellung galt) und wird nicht gelöscht, aber
auch nicht mehr referenziert. CLAUDE.md und alle Prompts verweisen ab
sofort ausschließlich auf planner-specs/.
